# Example env file — copy to .env.local and fill values
# Use server-only variable when possible: NEXT_GOOGLE_SHEETS_URL
NEXT_PUBLIC_GOOGLE_SHEETS_URL=https://script.google.com/macros/s/REPLACE_WITH_ID/exec
NEXT_GOOGLE_SHEETS_URL=https://script.google.com/macros/s/REPLACE_WITH_ID/exec
NEXT_PUBLIC_OWNER_EMAIL=owner@example.com
# Optional: set a secret to require an `x-contact-secret` header (set only on server)
# CONTACT_SECRET=your-strong-secret

